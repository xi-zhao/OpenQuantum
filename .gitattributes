# Keep repository text deterministic across research environments.
* text=auto

# Harness and validation scripts run in POSIX environments.
*.sh text eol=lf

# Windows launchers retain their native line endings.
*.bat text eol=crlf
*.cmd text eol=crlf
