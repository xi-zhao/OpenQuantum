[project]
name = "openquantum-qpanda-qubo-bridge"
version = "0.1.0"
description = "Pinned Python environment for the OpenQuantum QPanda QUBO MCP bridge"
requires-python = ">=3.11,<3.14"
dependencies = [
  "pyqpanda_alg==2.0.0",
]

[tool.uv]
package = false
